#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "dynamic_array.h"

int main(int argc, char **argv) {
  printf("Hello World!!!\n");
  printf("You really don't want to run this, however...");
  printf("Instead, you want to run the \"test harness\" which are the unit tests");
  return 0;
}
